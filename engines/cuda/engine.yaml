name: cuda
description: Use CUDA and Nvidia GPUs
vendor: Canonical Ltd
grade: stable
devices:
  all:
    - type: cpu
      architecture: amd64
      flags: [sse4_2, f16c, fma, avx, avx2]
    - type: gpu
      vendor-id: 0x10de # Nvidia Corporation
      vram: 5G
      # compute-capability: 5.0
memory: 4G
disk-space: 8G
components:
  - &s llamacpp-cuda
  - &m model-distill-qwen-7b-q4-k-m-gguf
configurations:
  server: *s
  model: *m
  http.base-path: v1
  gpu-layers: 29
